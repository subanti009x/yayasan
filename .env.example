# Development only. Copy to your local environment; do not commit the real values.
# Development only. Copy to your local environment; do not commit the real values.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=yayasan_cendekia
DB_USER=YOUR_DATABASE_USER
DB_PASSWORD=YOUR_DATABASE_PASSWORD

# Required in production; do not use a default password.
ADMIN_PASSWORD=CHANGE_THIS_TO_A_LONG_UNIQUE_PASSWORD
SESSION_SECRET=CHANGE_THIS_TO_A_RANDOM_64_CHARACTER_SECRET
